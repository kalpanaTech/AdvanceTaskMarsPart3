# Node modules (if using Newman with Node.js locally)
node_modules/

# Newman report files
newman/*.html
newman/*.json
newman/*.xml

# Postman environment files (if they contain secrets)
*.postman_environment.json

# OS-specific junk
.DS_Store
Thumbs.db

# Logs
*.log
